# Design debt register

Last reviewed: 2026-08-28

## Resolved in this pass

- Major — The hero proposition was semantically present but visually floated over the highest-frequency liquid-metal texture, weakening the first three-second explanation on mobile. A bounded proof plate now provides durable contrast and maps the promise to MEMORY → ACTION → EVIDENCE without competing with the wordmark or adding WebGL.
- Major — HELIX and QADAM formed two consecutive dense dark proof plates, flattening the pressure/release cadence and hiding the conceptual transition from recovery to evidence. A paper, scroll-owned proposition now creates one focused breath without changing the ten-chapter information architecture.
- Major — HELIX recovery relied only on native button activation, which did not survive the review browser's synthetic Enter/Space path. The control now explicitly handles both keys, ignores repeat, preserves the busy state, and reaches the verified receipt in fresh-origin production QA.
- Medium — The outlined HELIX title rendered at 259.2px against a documented 176px display maximum and dropped abruptly to QADAM at 151.2px. Its desktop clamp now resolves to 172.8px at 1440px, preserving distinction while returning to the token scale.
- Medium — Visual theme detection was coupled to the ten semantic navigation sections, so the new unnumbered handoff and rapid scrollbar jumps could leave chrome/theme state stale. Independent theme targets and a requestAnimationFrame viewport-centre fallback now keep both state machines accurate.
- Major — The hero poster was ready at first sample, but the loader enforced a 720ms minimum and kept its overlay mounted for roughly 1.51s. Ready content now releases immediately through a 320ms authored reveal / 360ms cleanup; the real-resource wait and 2.8s safety timeout remain intact. Repeated production samples remove the overlay in 396ms mobile and 376ms desktop.
- Medium — Primary desktop destinations and exact mobile menu links did not expose the chapter state already known to the rail/summary. Grouped desktop and exact mobile `aria-current` states now combine semantics with persistent non-colour indicators; Contact has a distinct active action treatment.
- Medium — The mobile header declared a 44px menu shell, but its bordered details element left the actual summary only 42px wide; visible name/email inputs were only 23px high. Border-compensated geometry now measures 44.4×44px for MENU, 44px for every expanded destination and short contact field, and 118px for the brief.
- Medium — The hero's `Scroll to enter` control repeated a familiar reference cue without providing meaningful information. It is now a direct `View selected systems` navigation action with verified safe landing geometry.
- Major — Active Field combined preview selection and external navigation in the same repository anchors, preventing touch users from exploring alternate visual states without leaving the site. It now has semantic project tabs, complete keyboard activation, a synchronized panel, a separate real repository CTA, and a mobile-specific 2×2 control deck.
- Major — Flagship evidence was presented as polished but static interface theatre. JARVIS now exposes four source-backed architecture states and QADAM exposes finding/source/privacy states through real semantic tab interactions; browser tests prove click and keyboard activation update the rendered proof.
- Major — The black liquid-metal opening handed off into another visually similar black scene, flattening the journey. An original authored raster now opens the surface into the pale evidence world with a single cobalt seam and explicit provenance.
- Major — The close ended as a useful form/footer but lacked an emotional peak. Contact now resolves into a full-viewport final invitation with a restrained identity echo, one CTA, and a still end state.
- Medium — The mobile menu trigger was an unlabeled plus and the project proof tabs initially moved keyboard focus without activating content. The control now reads `MENU`, and Arrow/Home/End navigation activates the matching tab panel in both proof scenes.
- Major — Missing `sizes` metadata caused Next.js to serve `w=3840` identity images into a 356px mobile hero and 55px About mark. Explicit responsive contracts now select `w=640` / 33.3KB and `w=64` / 1.1KB respectively; desktop selects fit-for-purpose `w=1080` and `w=96` candidates.
- Major — The contact API could write successfully to an ephemeral serverless filesystem and report delivery even though the message would not persist. Delivery now auto-selects a configured HTTPS webhook, uses file storage only on persistent/local Node hosts, and fails closed on known ephemeral hosts without a provider.
- Major — Contact validation returned one generic message, transient network failures had no timeout, and mobile focus could land beneath the fixed header. The form now exposes field-specific accessible recovery, brief length, preserved values, bounded timeout, retry copy, and post-render safe focus; the API returns field errors, reference IDs, Retry-After, request limits, and bounded rate-limit cleanup.
- Major — The public launch surface had no authored social card, favicon, Apple icon, or manifest identity. Deterministic launch assets now derive only from the owner-supplied liquid-metal poster, wordmark, and D mark; Metadata routes expose them with accurate descriptions and structured profile data.
- Major — README, brief, experience spine, world bible, gates, user flow, provenance, and third-party notices still described a removed astronaut/WebGL prototype. Current handoff documentation now matches the shipped ten-waypoint DOM/CSS/video architecture and labels the old defect ledger as historical.
- Medium — Launch caching covered only the primary film and poster, and baseline response hardening omitted frame and device-permission policies. Fallback media, identity assets, and manifest icons now receive long-lived cache rules; all responses declare nosniff, frame denial, strict referrer, and disabled camera/microphone/geolocation.
- Medium — Motion performance had not been closed with live long-page evidence. Desktop top/mid/footer and 390×844 top/mid now report zero offscreen-running CSS animations; the single hero video pauses outside its opening scene; three bounded production reloads remain stable at 848 elements, 4 images, 1 video, 0 canvas, and 0 iframe with an empty console.
- Low — An unused experimental `KineticField` component retained an infinite GSAP drift implementation even though it was absent from the rendered portfolio. The dead component was removed instead of claiming a runtime optimization that users would never experience.
- Major — The desktop rail link grid remained 58px wide inside a 44px collapsed disclosure, clipping waypoint hitboxes even after removing the old fixed 148px link width. Closed links now allocate only the 8px waypoint column; number and label columns are introduced only on hover/focus, and production geometry proves every collapsed hitbox is contained.
- Major — Flagship work could be mistaken for undisclosed client projects even though no client evidence was provided. The work opener now explicitly frames the portfolio as independent R&D and states which proof is public/measured versus an interaction prototype.
- Major — Signal blue was used on an oversized manifesto sentence, turning a state/action token into generic editorial decoration. The line now remains ink; blue is reserved for system signals, route progress, focus, and primary action.
- Major — Fixed chapter navigation competed with HELIX and Contact plates, while the mobile control floated over lower-page content. Desktop rail now exits during focal hero/project/contact chapters, and the mobile disclosure sits in the header safe-area with measured separation from brand and CTA.
- Major — The final action routed prospective clients into a public GitHub issue and required an account. Contact now provides a private in-site brief with native field semantics, server validation, honeypot, rate limiting, atomic feedback, and no public publication.
- Major — A standalone Capabilities chapter interrupted the documented light Method → dark experiment-index cadence. The same complete keyboard-operable accordion now closes Method as a subsection, restoring the authored sequence and reducing navigation from eleven to ten chapters.
- Major — At 390×844 the HELIX `RECOVERY / 04` caption collided with “Fail visibly. Recover deliberately.” after the recovery state changed. Mobile now reserves a discrete 78px footer plate and keeps the state badge inside the rail header.
- Major — Capabilities looked like an accordion but exposed no trigger-to-panel relationship, region labels, or arrow/Home/End navigation. It now follows the complete single-open accordion contract, collapses hidden panels out of interaction, and preserves the authored reveal motion.
- Major — HELIX exposed its entire changing ledger as a polite live region, which could announce multiple partial updates during the 900 ms recovery. Feedback is now one atomic status, the operation surface exposes `aria-busy`, and the control is explicitly tied to the affected region and simulation note.
- Major — The original cobalt and several low-opacity micro-labels fell below AA contrast in measured small-text combinations. The signal token is now a deeper electric cobalt with 5.19:1 against authored white, and essential micro-copy uses a consistent readable opacity without flattening the hierarchy.
- Medium — The floating mobile chapter control overlapped the primary contact CTA at the narrative peak. It now exits during Contact, leaving one clean action and an unobstructed final frame.
- Major — The visual loader exposed its rapidly changing percentage as a polite live region, risking dozens of screen-reader announcements while the underlying page was not marked busy. The loader now announces one atomic Loading portfolio → Portfolio ready lifecycle, hides decorative copy from assistive output, and owns an explicit main-content busy state.
- Major — Reduced-motion sessions hid the hero video but still attached and fully loaded the 2.93 MiB MP4. Hero media now starts as a poster-only shell, attaches motion after hydration only when motion and data preferences allow it, and records the active policy for runtime verification.
- Medium — The preloader coupled first-paint readiness to the hero's first decoded video frame even though a 75 KB poster already provides the complete composition. Poster readiness now releases that dependency, allowing progressive video enhancement behind the authored loading state.
- Major — Screen-reader heading navigation skipped the visually authored Thesis, Method, and Capabilities chapters because their display typography was built from paragraphs and controls. Each chapter now has a unique semantic H2 and labelled section without adding duplicate visible copy.
- Minor — The mobile brand target was only 34px wide and the header CTA 42px high. Both header actions now expose a 44px minimum hit area while retaining the same compact silhouette.
- Medium — At 844×390 the floating mobile menu overlapped the hero entry action by 1,122 square pixels, creating two competing controls in the same corner. A short-landscape clearance now preserves both actions and restores a single readable path into the portfolio.
- Medium — The mobile entry cue measured 42px high and the final Back to top link only 11px, creating two undersized touch targets at the beginning and end of the narrative. Both now provide a measured 44px minimum interactive area without changing the visual hierarchy.
- Medium — The 927px Capabilities chapter was visually distinct but absent from the navigation state machine, so rapid scrolling could leave the rail falsely labelled as Work or Method. It is now a first-class eleventh chapter with its own anchor, theme handoff, progress stop, and automated coverage.
- Medium — Eleven long-form chapters had no persistent spatial cue beyond the header. Desktop now has a compact chapter rail with a labelled current chapter, numbered overview on hover/focus, and a scroll-synchronised progress line; the rail yields to the existing mobile navigation below 960px.
- High — The Lusion references had not been closed out with a formal originality check. A deterministic asset/text inventory now records the supplied reference corpus, distinguishes owner-supplied identity media from inspiration, and documents the visual, structural, narrative, and interaction differences in `ORIGINALITY_AUDIT.md`.
- Medium — Hover feedback was locally styled but lacked a shared tactile language. A contextual signal cursor now communicates ENTER / OPEN / READ / VIEW / TALK across fine-pointer interactions, follows with directional velocity/spring physics, leaves a restrained speed trail, idles its RAF when settled, and disappears entirely for touch or reduced-motion modes.
- High — Client-side word splitting mutated React-owned nodes and could surface a Next.js `removeChild` failure during Fast Refresh. All animated word markup is now rendered by React, while GSAP only reads and animates those nodes; a live HMR update completed with no console errors.
- Medium — About and Contact previously formed a long, visually flat pale ending. About now has an authored operator map and evidence ledger; Contact closes on a high-contrast cobalt peak with one accurate primary action.
- Medium — The pale QADAM ending previously flowed into another pale explanatory section, flattening the portfolio's rhythm. Method is now a cobalt intermission with three oversized thresholds, a scroll-owned evidence statement, and a compact route card that hands off cleanly into the dark capabilities chapter.
- High — HELIX looked like a finished system without public implementation evidence. Its claim is now explicitly an interaction proof, and the chapter contains a real keyboard-operable fault → recovery → verified prototype with a visible simulation boundary.
- High — The Active Field chapter collapsed into a generic text list after three visually authored case studies. It is now a responsive interactive laboratory with four distinct 2D signals, explicit public-build status, hover/focus state, and scroll-owned motion.
- High — The flagship stories lacked a measurable outcome scene. QADAM now closes with reproducible evaluation metrics, test counts, explicit benchmark boundaries, and a link to the public methodology.
- Medium — The smooth-scroll owner stayed attached to the GSAP ticker while the document was hidden. Lenis and the ticker now stop and resume through one visibility lifecycle.
- Medium — Keyboard users had no bypass path around the fixed navigation and long hero. A focused skip link now jumps to selected work.
- Low — Preloader timers could outlive their component. All scheduled timers now share an explicit cleanup set.

## Round 20 resolved

- High — QADAM's portfolio evidence was source-backed but still visually reconstructed while a genuine public release walkthrough existed. The exact 48-second repository artifact is now local, provenance-linked, visitor-controlled, non-looping, and paired with the reproducible evaluation scene.
- Medium — The persistent Work action and hero shortcut stopped at an editorial opener rather than the first case study. They now land directly at JARVIS; natural scrolling retains the opener as a pacing beat.

## Round 21 resolved

- High — HELIX ended immediately after one interactive prototype frame, so its reasoning and proof boundary were materially thinner than the JARVIS and QADAM stories. A four-beat recovery-anatomy chapter now explains fault isolation, preserved state, owned retry, and receipt closure without implying a public production build.
- Medium — The new scroll-linked HELIX beats initially carried blur progression into the compact mobile composition. Mobile now renders every beat statically and sharply; desktop retains the restrained scrubbed progression.
- Minor — The forced/query reduced-motion path left the recovery meter at its empty CSS start state. `data-motion="reduced"` now renders the meter and all recovery beats in their complete final state.

## Round 22 resolved

- Medium — The four mobile HELIX ledgers stacked every field vertically, adding 732px to one proof chapter and weakening the pace. At 390×844 they now use compact three-column technical plates: the chapter falls from 3,978px to 3,246px while preserving every label and value.
- Medium — Desktop-only HELIX scrub scenes were still instantiated on mobile even though CSS forced their final visual state. GSAP `matchMedia` now creates the meter and four beat timelines only from 641px upward; mobile production exposes no inline GSAP state and retains zero offscreen-running CSS animations.
- Major — A direct reload at the new HELIX recovery hash could be returned to the hero when the preloader released after the browser's initial anchor alignment. Hash targets now realign on `portfolio:ready`; fresh desktop and mobile production loads place the recovery section at 91px and 78px respectively, below fixed chrome.
- Medium — The HELIX footer's only narrative action skipped its own new proof model and jumped to Experiments. It now continues into `#helix-recovery-model`, with verified mobile landing geometry.

## Round 23 resolved

- High — The decorative chrome signature was memorable but not reliably legible as a name, so the opening failed the three-second identity requirement. A visible semantic `Daniyal Bauyrzhan` H1 and `Independent AI systems builder` role now lead the signature, proposition, and action on both desktop and mobile.
- High — The nominally compact mobile HELIX anatomy still consumed 3,246px and treated each decision as an oversized editorial scene. It is now a 1,757px static technical ledger with a 310px intro and 293–321px beats; multiple complete decisions fit within one viewport and all twelve proof fields remain intact.
- Medium — Current implementation acceptance was fragmented across old self-QA and unavailable critic retries. Round 23 now has a complete fresh independent result: brief pass after rebuild, system pass after rebuild, and craft preference for our matched desktop/mobile set.

## Round 24 resolved

- High — Persistent mobile chrome repeatedly crossed oversized headlines and proof while the visitor read downward, weakening focal scenes throughout the 23,147px journey. One RAF-batched scroll-intent state now yields both header and disclosure after deliberate descent, recalls them after a short upward gesture, and keeps them available at the top, while open, during navigation focus, and under reduced motion.
- Medium — The first chrome-hide implementation used the individual `translate` property, but GSAP's inline transform ownership overrode it in production. The final state moves chrome through `top`, giving navigation and scene choreography independent properties and a browser-verified contract.
- Medium — Long-page visual review depended on manual screenshots. A reusable settled-capture stitch helper and manifests now preserve matched desktop/mobile evidence without adding runtime code.

## Round 25 resolved

- High — The 23,000px portfolio exposed three evidence-heavy case studies but offered no comparable proof overview at the Work entry, forcing a time-constrained jury to enter the first long chapter before understanding the set. Selected Work now contains three direct, labelled case-study routes within the original viewport.
- High — Two text-led proof-route iterations still treated evidence as small metadata and lost the craft comparison. The accepted route stages three truthful mini instruments—operation receipt, recovery path, and reproducible evaluation—as the visual payoff without adding a new chapter or invented result.
- Medium — Mobile connection policy respected reduced motion and save-data but would still attach the 3.07MB hero film on reported 2g/3g connections. Network Information API tiers below `4g` now keep the complete poster-only composition.

## Round 26 resolved

- High — Public-source case studies linked only broad repository destinations, so a time-constrained reviewer still had to search for the file that supported each adjacent claim. JARVIS and QADAM now expose eight exact source receipts beside their proof scenes.
- Medium — Public metrics and architecture claims had no maintained local provenance ledger. `SOURCE_EVIDENCE_REGISTER.md` now records confidence, exact primary source, rejected overclaims, the verification date, and the required update rule.
- Medium — Adding source links risked turning the case studies into documentation. The accepted implementation keeps receipts in the existing numbered grid language; desktop uses a single subordinate band and mobile a compact 2×2 proof deck. Fresh craft review selected the portfolio over a generic competent alternative.

## Round 27 resolved

- Medium — The 3.07MB landscape hero film was the only art direction delivered to phones, making the opening depend on a generic CSS crop and paying the desktop payload on narrow screens. A centered 404×720 portrait derivative now serves phones at 516,789 bytes with no audio and measured SSIM 0.989838; the desktop master remains unchanged.
- Low — Responsive media behavior was previously inferred from CSS alone. Production browser evidence now records the selected mobile/desktop `currentSrc`, poster-only reduced-motion state, offscreen pause, and empty warning/error console.

## Round 28 resolved

- Low — The desktop hero reacted only through the media layer, so the readable identity and proof surface felt visually glued to the film. A bounded two-layer pointer response now gives the content a 2–5px depth relationship while preserving GSAP ownership, reduced-motion parity, and reset behavior.

## Round 47 resolved

- High — The Active Field carousel repeated projects already presented in the 14-item repository atlas, weakening narrative variety and making the lower page feel longer without adding evidence. It is replaced by an observable-operation protocol with six distinct method states and an explicit simulation disclosure.
- High — The carousel marquee continued running at every sampled scroll position even when 8,000–21,000px outside the viewport. The owner was removed; production sampling now reports zero actually running offscreen CSS animations after settling at top, mid-page, and footer.
- High — The atlas attached separate ScrollTriggers to each card and each poster, while Infinite Canvas updated eighteen frame tweens on every scrub tick. Atlas ownership is reduced from 28 to 14 triggers and the image field now moves through one parent-plane transform.
- Medium — Large text swaps and fixed chrome depended on animated blur/backdrop-filter layers. State changes now use transform and opacity, and fixed navigation surfaces use high-opacity authored fills that preserve hierarchy without continuous backdrop repaint.
- Medium — The exact directional cursor kept its physics RAF alive indefinitely at rest. Constants and state behavior remain intact, but scheduling now sleeps after convergence, wakes on pointer/scroll/state changes, and pauses on hidden documents.
- Major — Mobile viewport emulation exposed the desktop header's `translateX(-50%)` retained in an inline GSAP transform after crossing the 760px breakpoint, pushing the brand outside the viewport. The mobile header now explicitly clears transform ownership while preserving the intro choreography.

## Open debt
- Medium — The canonical public origin is intentionally not invented. Set `NEXT_PUBLIC_SITE_URL` on the final non-Vercel host (Vercel production URLs are detected automatically), then recheck canonical, Open Graph, and Twitter URLs on the deployed domain.
- High — No named client, adoption figure, testimonial, award placement, or live-user outcome has been supplied or found in public project evidence. The portfolio now avoids client theatre, but genuine third-party proof remains the largest content gap; add only verified material from the owner.
- Medium — The durable delivery adapter is implemented, but no owner-controlled production webhook/token or persistent-volume path has been supplied. Configure and verify one before public deployment; known ephemeral hosts intentionally return an unavailable state until then.
- Medium — JARVIS has rich architectural proof but no public benchmark comparable to QADAM. Add only when a reproducible evaluation exists; do not invent one.
- Medium — HELIX now has an honest local interaction proof, but still lacks a public implementation repository. Keep the prototype label until source evidence exists.
- Low — The project is intentionally media-led; the 8-second 1280×720 desktop master is 3.07MB and the final deployment still needs real-device checks on low-power iOS and Android hardware. The phone derivative reduces transfer cost, but field data remains a launch gate.
- Low — A final public launch should include field performance data rather than relying only on local profiling.

## Guardrails

- New visual effects must preserve the single GSAP/Lenis motion owner.
- Every new case-study claim needs a source, artifact, or clearly labelled concept status.
- Reduced-motion, keyboard order, focus visibility, and mobile layout remain release gates.
